# Fichier : .htaccess à la racine du projet

RewriteEngine On

# Ne réécrit pas les requêtes pour des fichiers physiques (css, js, images)
RewriteCond %{REQUEST_FILENAME} !-f
# Ne réécrit pas les requêtes pour des dossiers physiques
RewriteCond %{REQUEST_FILENAME} !-d

# Redirige tout le reste vers index.php
# Le chemin vers index.php est relatif au .htaccess, donc il n'y a pas besoin de sous-dossier ici.
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]